services:
  postgres:
    image: postgres
    restart: always
    env_file: 
      - ./.env.dev
    ports:
      - 5432:${POSTGRES_PORT}
    environment:
       POSTGRES_DB: ${POSTGRES_DB}
       POSTGRES_USER: ${POSTGRES_USER}
       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD} 